This project is a Hobby 16 bits OS in full ASM, limited to a basic shell with only nano and cat to a static location, so can only have one file at a time, and clear.
It has worked for practicing for a next version, where I'll focus on doing an OS in C, still no GUI is intended.
